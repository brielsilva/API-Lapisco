const db = require('../../../db/db');

class ContactsRepository {
	create({name, email, phone, gender, picture, birthday}) {}
	findById(id) {}
	findAll() {}
	findByName() {}
	findByEmail() {}
	update(id) {}
	delete(id) {}
}